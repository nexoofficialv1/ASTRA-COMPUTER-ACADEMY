ASTRA Computer Academy v1.0.2 Test Hotfix

Fixes:
1. Release contract no longer reads PNG/binary Android resources as UTF-8 text.
2. Release version assertion is semantic-version based, so patch releases do not stale the test.
3. excel_workflow practicals are validated with their actual workflow schema.
4. Practical integrity test now handles every current practicalKind explicitly.
5. pubspec build version updated to 1.0.2+12.
6. GitHub APK artifact name updated to v1.0.2.

Apply by extracting this ZIP directly into the repository root, then commit and push.
